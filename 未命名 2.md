第四问本质上是在第三问模型上再加控制平滑性约束，也就是限制 GPU 负载和传输速率不能频繁大幅波动。

  ---
  1. 第四问新增内容

  第三问已经包含：

  - 分时电价；
  - BESS 电池储能；
  - 冷却功率惯性；
  - 日处理量约束；
  - GPU 负载、传输速率上下限。

  第四问新增：

  $$
  |x_{t+1}-x_t|\le 8
  $$

  $$
  |y_{t+1}-y_t|\le 300
  $$

  含义是：

  - 每小时 GPU 负载变化不能超过 8 个百分点；
  - 每小时传输速率变化不能超过 300 Mbps。

  ---
  2. 这两个约束和第三问中已有约束的关系

  2.1 GPU 变化率约束

  第三问已有冷却惯性：

  $$
  |z_{t+1}-z_t|\le0.2
  $$

  同时：

  $$
  z_t\ge1.2+0.12(x_t-60)
  $$

  如果冷却功率一直贴着最低需求，那么：

  $$
  \Delta z \approx 0.12 \Delta x
  $$

  因此：

  $$
  |\Delta x|\lesssim \frac{0.2}{0.12}\approx1.67
  $$

  也就是说，冷却惯性本身已经比第四问的 GPU 变化率 8% 更严格。

  所以第四问中的 GPU 变化率约束：

  $$
  |x_{t+1}-x_t|\le8
  $$

  大概率不会成为真正起作用的约束。

  真正起作用的仍然是冷却功率变化率约束。

  ---
  2.2 传输速率变化率约束

  第三问中最优解里，传输速率基本全天为：

  $$
  y_t=1200
  $$

  如果第四问仍然如此，那么：

  $$
  |y_{t+1}-y_t|=0\le300
  $$

  所以传输速率变化率约束也大概率不活跃。

  也就是说，第四问可能与第三问结果非常接近，甚至完全相同。

  ---
  3. 第四问的模型变化

  在第三问模型基础上增加以下约束即可。

  GPU 负载变化率约束

  对 $t=1,\ldots,23$：

  $$
  x_{t+1}-x_t\le8
  $$

  $$
  x_t-x_{t+1}\le8
  $$

  如果考虑 24 小时循环调度，还应加首尾约束：

  $$
  x_1-x_{24}\le8
  $$

  $$
  x_{24}-x_1\le8
  $$

  传输速率变化率约束

  对 $t=1,\ldots,23$：

  $$
  y_{t+1}-y_t\le300
  $$

  $$
  y_t-y_{t+1}\le300
  $$

  同样加入首尾周期约束：

  $$
  y_1-y_{24}\le300
  $$

  $$
  y_{24}-y_1\le300
  $$

  ---
  4. 是否需要改目标函数？

  不需要。

  第四问的目标仍然是最小化日电费：

  $$
  \min \sum_{t=1}^{24} c_tP_{grid,t}
  $$

  变化只体现在可行域缩小。

  ---
  5. 求解方法

  第四问仍然可以用 Gurobi。

  模型类型没有本质变化：

  - 双线性处理量约束 $x_ty_t$；
  - BESS 的二进制变量；
  - GPU 功耗 PWL；
  - 冷却惯性线性约束；
  - 新增的 $x_t,y_t$ 变化率线性约束。

  因此仍属于：

  ▎ 混合整数非凸二次约束规划

  Gurobi 继续设置：

  m.Params.NonConvex = 2

  即可。

  ---
  6. 预期结果

  我预计第四问有两种可能：

  情况 A：结果与第三问完全一致

  如果第三问解本身已经满足：

  $$
  |x_{t+1}-x_t|\le8
  $$

  和：

  $$
  |y_{t+1}-y_t|\le300
  $$

  那么第四问不会改变最优解。

  目前第三问结果里：

  - $y_t$ 全部为 1200，变化率为 0；
  - $x_t$ 每小时变化基本是 1.67 左右，明显小于 8；

  所以大概率第四问与第三问完全一致。

  情况 B：首尾周期处需要调整

  第三问的首尾冷却功率已满足：

  $$
  |z_1-z_{24}|\le0.2
  $$

  但还要检查：

  $$
  |x_1-x_{24}|\le8
  $$

  第三问里大约：

  - $x_1=74.33$
  - $x_{24}=72.67$

  差值约 1.66，也满足。

  所以更可能是情况 A：第四问结果不变。

  ---
  7. 论文里怎么写才不显得“没变化”

  即使结果不变，也不是没有意义。

  可以写成：

  ▎ 在第三问最优调度结果中，由于冷却系统惯性约束已经对 GPU 负载变化形成了更严格的限制，GPU 负载的小时变化幅度最大仅约 1.67%，小于题目四给出的 8% 限制；同时传输速率在最优解中保持 1200 Mbps 不变，其变化率为
  ▎ 0。因此，问题四新增的稳定性约束未成为活跃约束，最优调度方案与问题三保持一致。

  这反而是一个很好的结论：

  ▎ 第三问中的冷却惯性约束已经隐含保证了设备运行平稳性。

  ---
  8. 第四问建议写法

  可以按这个逻辑：

  1. 在第三问模型基础上引入 $x_t,y_t$ 变化率约束；
  2. 说明这些约束是设备寿命和控制稳定性的数学表达；
  3. 用 Gurobi 重新求解；
  4. 对比第三问和第四问：
    - 若结果相同：说明新增约束不活跃；
    - 若略有变化：说明稳定性约束带来一定电费损失；
  5. 给出最大变化率检查表。

  ---
  6. 第四问代码改动

  直接复制 Q3/solve_q3.py 为：

  Q4/solve_q4.py

  然后在冷却惯性约束后加入：

  for t in range(HOURS - 1):
      m.addConstr(x[t + 1] - x[t] <= 8)
      m.addConstr(x[t] - x[t + 1] <= 8)

      m.addConstr(y[t + 1] - y[t] <= 300)
      m.addConstr(y[t] - y[t + 1] <= 300)

  m.addConstr(x[0] - x[HOURS - 1] <= 8)
  m.addConstr(x[HOURS - 1] - x[0] <= 8)

  m.addConstr(y[0] - y[HOURS - 1] <= 300)
  m.addConstr(y[HOURS - 1] - y[0] <= 300)

  其余部分基本不变。

  ---
  结论

  第四问的关键不是重新设计一套模型，而是：

  ▎ 在第三问动态优化模型基础上加入 GPU 负载和传输速率变化率约束，并检验这些新增稳定性约束是否改变最优调度。

  从目前第三问结果看，第四问大概率与第三问相同，原因是冷却惯性已经比 GPU 变化率约束更严格。