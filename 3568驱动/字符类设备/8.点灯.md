首先，先去开发板的原理图里找对应的引脚
找到引脚之后，再去RK3568的手册里找对应的寄存器
需要配置三个东西
- 引脚复用这个引脚是复用成GPIO，还是PWM....
- 方向 输入还是输出
- 数据 是0还是1
找对应的基地址，偏移地址
可以先在终端里看一下配置成了什么
```c
io -r -4 0xfdd60000
```
-r 指read          -4 指查看4字节  4* 8等于32，所以就等于查看了寄存器的32位
输出的是16进制，自己在计算机里面转换成32位二进制    