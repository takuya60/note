编译了一遍完整的buildroot
```c
./build.sh BoardConfig-rk3568-atk-atompi-ca1.mk

./build.sh all
```
这里的build all，指的是kernel rootfs uboot全都编译

```c
ls -lh buildroot/output/rockchip_rk3568/images/rootfs.*
```
![[Pasted image 20260304173323.png]]

```c
ls -lh kernel/arch/arm64/boot/Image
```
这个是内核镜像的大小  22 MB