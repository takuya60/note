在内核配置的menuconfig里面搜索USB_CONFIGFS_F_HID

## 运行ConfigFS 挂载脚本出错
因为UDC设备被rockchip自己挂载的默认驱动占用
尝试使用 `echo ""` 写入 UDC 节点去强制解绑，但发现仅仅过了一秒钟系统又报 busy
所以直接去系统启动序列`/etc/init.d/`找到厂家默认的
```c
cd /etc/init.d/
ls *usb* *adb* *rockchip* 2>/dev/null

mv S50usb.sh K50usb_disable
```
直接给他重命名掉，然后重启板子reboot