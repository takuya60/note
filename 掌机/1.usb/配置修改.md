### 设备树
```c
&usb20_otg { // 节点名字可能略有不同，如 &usbdrd 或 &dwc2
    status = "okay";               // 确保该硬件模块被启用
    dr_mode = "peripheral";        // 关键！将其强制设为外设模式，而不是 "host" 或 "otg"
    // phys = <&u2phy0_otg>;       // 通常会有 phy 物理层节点的引用，保持默认即可
};
```

### 内核
![[Pasted image 20260331225810.png]]