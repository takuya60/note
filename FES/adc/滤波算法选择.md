如果把窗口设置为9，代表有9个点
## 1.滑动平均
直接对这九个点求均值
坏处是会在跳变的时候变成梯形
## 2.去极值平均
去掉最大值与最小值，对剩下的求平均
还是会变成梯形
```c
// 先定义窗口大小
#define channel_num 1
#define BUFF_WIN_SIZE 16
static uint16_t history[chanel_num][BUFF_WIN_SIZE]
uint8_t
// 从dma搬运过来的数据里面取值

```

## 3.中值滤波
对这九个值进行排序，取中点

