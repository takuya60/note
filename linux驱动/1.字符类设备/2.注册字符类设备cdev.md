注册字符类设备需要用cdev结构体
cdev结构体里有一个file_operations结构体指针，所以我们需要先自己创建一个
struct  file_operations m_ops 
然后在cdev的init的时候，把这个file_operations传进去，，cdev才算完整
然后在add的时候，让cdev对号入坐进我们准备好的设备号
character device
```c
struct cdev m_dev;
static int moudle_init(void){
// 初始化，告诉cdev对应的操作函数
// fops ：file operations 结构体
m_dev.owner=THIS_MOUDLE;
dev_init(&cdev,&fops);
// 正式添加到系统中
dev_add(&cdev,dev_id,1);
....
}
static void moudle_exit()
{
	cdev_del(&m_dev);
	unregisdter_chrdev....;
}
```