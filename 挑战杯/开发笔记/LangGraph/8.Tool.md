 LangGraph 也支持工具调用。
  常见用法：
  from langgraph.prebuilt import ToolNode
  例如你定义一个工具：
  def search_knowledge_base(query: str):
      return "检索结果..."
  然后包装成 ToolNode。
  不过在你们项目里，RAG 检索更建议单独写成：
  retrieval_node
  而不是一开始就复杂地做成工具调用 Agent。
  原因是：
  比赛项目需要流程稳定、可解释、可控
  如果让大模型自己决定什么时候调用工具，演示时不如固定流程稳定。